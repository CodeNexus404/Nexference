{
  "name": "nexference",
  "version": "1.0.0",
  "description": "Nexference — AI Coding Gateway Switcher. Browse free models, pick one, apply to Claude Code or any client.",
  "type": "module",
  "scripts": {
    "dev": "node --watch server.js",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
