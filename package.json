{
  "name": "nexference",
  "version": "1.5.0",
  "description": "Nexference — Universal AI provider & runtime workspace. Manage cloud providers, local runtimes, models, and client configuration.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "node --watch server.js",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
